import { useMemo, useState } from 'react'
import { JobCard } from '../components/jobs/JobCard'
import { jobs } from '../data/jobs'
export function JobsPage() { const [query, setQuery] = useState(''); const result = useMemo(() => jobs.filter(x => `${x.title} ${x.company} ${x.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())), [query]); return <section className="page section"><p className="eyebrow">KHÁM PHÁ CƠ HỘI</p><h1>Tìm công việc tiếp theo của bạn</h1><div className="filter-bar"><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Tìm theo vị trí hoặc kỹ năng"/><select><option>Tất cả địa điểm</option><option>Hà Nội</option><option>Hồ Chí Minh</option></select><button className="button">Tìm kiếm</button></div><div className="content-grid"><aside className="filters"><b>Bộ lọc</b><label><input type="checkbox"/> Làm việc từ xa</label><label><input type="checkbox"/> Toàn thời gian</label><label><input type="checkbox"/> Mới đăng hôm nay</label></aside><div><p className="result-count">Có {result.length} việc làm phù hợp</p><div className="job-list">{result.map(job => <JobCard key={job.id} job={job}/>)}</div></div></div></section> }
